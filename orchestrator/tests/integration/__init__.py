# orchestrator.tests.integration — MOCK-02 end-to-end integration tests
# Requires: anvil (always), Postgres + Redis (optional — tests skip cleanly when absent)
# Run with the dev stack: make up && uv run pytest tests/integration -m integration
